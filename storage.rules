rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Rule for main images
    match /images/{userId}/{fileName} {
      allow write: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null;
    }

    // Rule for thumbnails
    match /thumbnails/{userId}/{fileName} {
      allow write: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null;
    }
  }
}